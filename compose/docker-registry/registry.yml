version: "3.0"

services:
  registry:
    container_name: registry
    restart: always
    image: registry:2.7
    ports:
      - 5000:5000
    volumes:
      - registry-data:/var/lib/registry

volumes:
  registry-data: {}
